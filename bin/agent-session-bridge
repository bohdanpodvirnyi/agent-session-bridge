#!/usr/bin/env node

import { main } from "../packages/cli/dist/cli/src/index.js";

main().then(
  (code) => {
    process.exitCode = code;
  },
  (error) => {
    console.error(error);
    process.exitCode = 1;
  },
);
